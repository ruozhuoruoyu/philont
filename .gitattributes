# SKILL.md 必须保持 LF 行尾。
#
# 我们的 frontmatter 解析器虽然在运行时也兼容 CRLF(loader.ts 会先归一化),
# 但仓库层强制 LF 防御深度更可靠 —— Windows git 默认 core.autocrlf=true
# 会把 LF→CRLF 检出,新增 SKILL.md 的人也会因此踩坑。
*.md       text eol=lf
SKILL.md   text eol=lf

# 源代码文件 LF
*.ts       text eol=lf
*.js       text eol=lf
*.json     text eol=lf
*.yaml     text eol=lf
*.yml      text eol=lf

# 二进制文件不动
*.png      binary
*.jpg      binary
*.gif      binary
*.zip      binary
