{
  "name": "@agent/launcher",
  "version": "0.1.0",
  "type": "module",
  "description": "Philont supervisor: serves the web-ui, manages config, and starts/stops/restarts the agent server.",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc"
  },
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
